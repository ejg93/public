// 본문에 나오는 용어의 뜻. 키가 본문에 그대로 있으면 렌더러가 밑줄을 긋고 마우스를 올렸을 때 뜻을 띄운다.
// 긴 키부터 맞춰 보므로 `RFC 9457 Problem Details` 가 `RFC 9457` 보다 먼저 잡힌다.
// 뜻은 한 문장으로 쓴다. 길면 툴팁이 화면을 덮는다.
export const GLOSSARY: Record<string, string> = {
  '청크':
    '작업을 쪼갠 단위. 파일 한두 개와 커밋 하나로 끝나게 자르고, 번호 뒤에 글자를 붙여 이미 있는 청크 사이에 끼워 넣는다. (토큰 주간 초기화가 아까워서 정의해둔 작업 단위)',
  'RFC 9457 Problem Details':
    'HTTP API 의 오류 응답 본문을 일관된 형식으로 표현하려고 제정된 IETF 표준 규격. type·title·status·detail·instance 다섯 칸으로 오류를 적는다',
  'RFC 9457':
    'HTTP API 의 오류 응답 본문을 일관된 형식으로 표현하려고 제정된 IETF 표준 규격',
  'application/problem+json':
    '파일 이름이 아니라 미디어 타입이다. 응답의 Content-Type 헤더에 이 값이 붙어서, 본문을 열기 전에 오류 응답인 것을 알린다. 평소 붙는 application/json 자리를 대신한다',
  'Bean Validation':
    '자바 표준 검증 규격(jakarta.validation). 요청 객체 필드에 애너테이션을 붙여 두면 컨트롤러에 들어가기 전에 값의 형태를 검사한다',
  'RBAC':
    'Role Based Access Control. 사람에게 권한을 직접 주지 않고 역할에 권한을 묶은 뒤 사람에게 역할을 주는 방식',
  'role_permission':
    '역할과 권한을 잇는 표. 어디까지 보이는지(스코프)와 허용·거부 여부를 이 행이 들고 있다',
  '스코프':
    '권한이 닿는 범위. own 은 자기 것만, seller 는 속한 셀러 것까지, all 은 전부다',
  'check 제약':
    '한 행의 값이 조건을 만족해야만 저장되게 DB 에 거는 규칙. 앱을 거치지 않고 넣어도 막힌다',
  '지연 트리거':
    '트랜잭션을 커밋하는 시점에 검사하는 DB 트리거. 여러 표에 걸친 조건처럼 행 하나만 봐서는 못 거는 규칙에 쓴다',
  'enum':
    '정해진 값 목록만 가질 수 있는 자바 타입. 목록에 없는 값을 쓰면 컴파일 단계에서 걸린다',
  'ADR':
    'Architecture Decision Record. 무엇을 왜 그렇게 정했고 무엇을 버렸는지 한 건씩 남기는 결정 기록',
  'SpotBugs':
    '자바 바이트코드를 훑어 버그 패턴을 찾는 정적 분석 도구',
  'gitleaks':
    '저장소에 비밀번호나 토큰 같은 비밀값이 섞여 들어갔는지 훑는 도구',
  'CodeQL':
    '코드를 질의 대상으로 바꿔 취약점이 흘러가는 경로를 찾는 깃허브의 정적 분석 엔진',
  'Dependabot':
    '의존 라이브러리의 새 판과 취약점 경보를 감지해 갱신 PR 을 자동으로 올리는 깃허브 기능',
  'CI':
    'Continuous Integration. 커밋이나 PR 이 올라올 때마다 빌드와 검사를 서버가 자동으로 돌리는 것',
  'PR':
    'Pull Request. 바꾼 코드를 본 가지에 합치기 전에 올려서 검사와 리뷰를 받는 단위',
  'diff':
    '바뀐 줄만 추려 보여 주는 비교 결과. 리뷰는 이것을 읽는다',
}
