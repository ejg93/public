# 개발용html 폴더 — 파일 현황 & 개선 아이디어

> 2026-07-10 기준. 전부 단일 파일·외부 CDN 없음(폐쇄망 반입 가능 구조) — 이 원칙 유지할 것.

## 파일 목록

| 파일 | 용도 | 상태 |
|------|------|------|
| `index.html` | 런처 — 도구 4개 카드, 새 탭 열기 | 2026-07-10 신규 |
| `dev_tools.html` | 카멜 변환 / JSON 포매터 / 날짜 계산 / INSERT 생성 / 정규식 / Base64·data URI / SQL 페이징(방언 5종) / URL / JWT | 사용 중 |
| `sql_snippets.html` | SQL 스니펫 모음 (방언 5종 전환, 검색, 즐겨찾기, 바인드 하이라이트) | 사용 중 |
| `특수문자_모음.html` | 특수문자 클릭 복사 + IME 입력법 + 즐겨찾기(우클릭) + 조합 모드 + 선문자 | 사용 중 |
| `jsp_formatter.html` | JSP/HTML 줄정리 (들여쓰기 재배치, 스크립틀릿 `{ }` 인지, 보존 블록 원형 유지) | 사용 중 |
| `doc/` | 도구 근거자료 폴더 — `가운뎃점_공문서_표준.md`(공문서/법령 가운뎃점 코드 표준) | 2026-07-15 신규 |
| `산출물_sql.html` | 공공 데이터 산출물 9종(DB정의서·테이블·컬럼·관계·표준용어/단어/도메인/코드·연계목록) 문서별 추출 SQL + 양식 열 명세 + 수기 잔여 규칙. 방언 탭 5종(Tibero/Oracle·MySQL/MariaDB·PostgreSQL·SQL Server·Sybase ASE), 스키마명 일괄 치환(localStorage), 쿼리 편집 모드 | 2026-07-10 신규 |

## 완료 (2026-07-10)

- [x] dev_tools: Base64 탭 파일 → data URI (드래그&드롭, 100KB 초과 경고)
- [x] dev_tools: JSON 16자리 이상 숫자 정밀도 경고 (파싱 전 감지, 노란 배지)
- [x] dev_tools: 마지막 사용 탭 기억 — **원래부터 구현돼 있었음** (메모가 낡았던 것)
- [x] dev_tools: 정규식 프리셋 — 사업자번호 하이픈 무관, 법인번호, 전화(지역), 주민번호(전체) 추가.
      정규식 테스트 입력값은 localStorage에 저장 안 됨 (개인정보 안전)
- [x] dev_tools: INSERT 생성기 DATE 더미값 `TO_DATE('YYYY-MM-DD HH24:MI:SS')` 통일 (시분초 랜덤 포함)
- [x] sql_snippets: 락/세션/인덱스 목록·사용 현황 — **이미 있었음** (메모가 낡았던 것).
      실제 추가분: 컬럼 목록에 코멘트 조인(Oracle), 최근 변경 객체 LAST_DDL_TIME(Oracle),
      그룹핑 카운트 템플릿(3개 DB 공통)
- [x] sql_snippets: 바인드 하이라이트 (`#{param}` / `#param#` / `:param` 주황색, textarea → pre 전환)
- [x] 특수문자: 즐겨찾기(우클릭 토글, localStorage, 상단 고정) / 조합 모드(클릭 누적 → 한 번에 복사) / 선문자 카테고리
- [x] jsp_formatter: 알려진 한계 3개 문서화 (파일 상단 주석 + 화면 하단 안내)
- [x] index.html 런처 생성
- [x] 전 파일 changelog 상단 주석 도입 (파일명 버전업 대신)
- [x] CDN 검사 통과 (`script src=` / `link href=http` 0건, 2026-07-10)
- [x] 산출물_sql.html 신규 + 런처 등록 + CDN 검사 통과 (2026-07-10).
      스키마명·기관명은 입력칸/자리표시자 방식, 방언 5종 탭

## 완료 (2026-07-15)

- [x] 특수문자: "가운뎃점·구분점" 카테고리 추가 —
      `· ・ • ∙ ‧ ⋅ ／ ｜ 、 ， ㆍ`. 가운뎃점 변형·구분점 모음
- [x] doc 폴더 신규 + `doc/가운뎃점_공문서_표준.md` 작성 —
      표준이 둘로 갈림: 일반 문서 `·`(U+00B7, 국립국어원 문장부호) vs 법령 조문 `ㆍ`(U+318D, 법제처 법령정비기준).
      HWP Shift+ㅏ=U+318D / Alt+0183=U+00B7. 혼동 유니코드 7종 대조표 포함

- [x] 산출물_sql: 부록 문서 카드 `90 데이터 품질 진단·정제` 신규 (2026-07-15) —
      사이드바 '부록' 그룹 추가. 쿼리 8종 × 방언 5종:
      [진단] 포맷 시그니처 분포(TRANSLATE 접기) / PK 없는 테이블 / 후보키 중복·NULL /
      NULL 대체값 분포 / 공백·개행·비숫자·전각 혼입 / 코드값 대사,
      [정제] 오염 날짜→YYYYMMDD CASE(실사례: VARCHAR(14) 절단 + ISO·14자리·Sybase 영문 혼재),
      [검증] 실패행·비정상 월일. 원칙: 진단→정제→검증 순, CASE ELSE는 NULL 고정.
      방언 주의 반영: MySQL REGEXP_REPLACE match_type 'c'(ci collation), MSSQL TRANSLATE 2017+ & COLLATE _BIN,
      Sybase TRY_CONVERT 없음(배치 중단 경고)·sysindexes.status&2048=PK. JS 파싱·CDN 0건 검증 완료

## 남은 개선 후보

### jsp_formatter.html
1. 정리 전/후 diff 뷰 — 공백 외 변경 0 보증 표시 (규모 있음, 별도 작업)
2. 파일 드래그&드롭 열기, 여러 파일 일괄 처리

### dev_tools.html
1. JSON 큰 숫자를 경고에서 한발 더 — 문자열 기반 파서로 정밀도 자체 보존 (현재는 감지·경고만)

## 공통 원칙

1. **버전 관리**: 파일명에 버전(`_v2`, `_v3`) 붙이지 말 것 → changelog 누적
   (2026-07-11~ `산출물_sql.html`은 changelog를 이 메모로 이관 — 아래 「산출물_sql.html 변경이력」.
   나머지 파일은 아직 파일 상단 주석 사용)
   (OneDrive 충돌 사본 생기면 파일명 버전은 더 헷갈림)
   2026-07-10 기존 버전 표기 일괄 제거: `dev_tools_v3.html` → `dev_tools.html`,
   `sql_snippets_v2.html` → `sql_snippets.html`. 파일명 바꾸면 `index.html` TOOLS 배열도 같이 고칠 것
2. **폐쇄망 반입 체크**: 신규 파일 추가 때마다 CDN 검사
   (`grep -E '<script src=|link.*href="http' *.html` — 0건이어야 함)
3. **접속정보·계정·실데이터 예시 절대 금지** — OneDrive 동기화 폴더라 유출면이 됨. 지금도 없음. 유지.
4. 새 도구 추가 시 `index.html`의 `TOOLS` 배열에 한 줄 추가

---

## 산출물_sql.html 변경이력 (파일 상단 주석에서 이관, 2026-07-11)

**2026-07-10**
- 최초 작성 — 공공 데이터 산출물 9종 문서별 추출 SQL + 기입 가이드. 전역 스키마명 치환(localStorage), 편집 모드, 복사
- 방언 탭 5종 확장(Tibero/Oracle·MySQL/MariaDB·PostgreSQL·SQL Server·Sybase ASE). 쿼리 `{방언키:쿼리}` 매트릭스
- AI 교차검증 1차: Oracle 데이터길이 CASE 교정(미지정 NUMBER=NULL, 문자형 CHAR_LENGTH), 05 컬럼당 1행 통일+뷰제외, '(코멘트 없음)' 표기, 스키마 기본값 제거+경고, PG `relkind IN('r','p') AND NOT relispartition`, Sybase 스키마칸 비활성화, 09 연계 수기항목 명시, ASE systabstats MAX+GROUP BY
- 파생: 01 PG 테이블수 pg_class 기준, MSSQL max_length 바이트 보정(N타입/2, max=-1)+소수점열, 05 도메인후보 길이표기+코멘트종류수 CAST 4000, PG 파티션 reltuples 주석, 02 통계 근사치 경고

**2026-07-11**
- 2차: 02 MySQL UPDATE_TIME NULL 리셋 경고 / 04 MSSQL FK 한글열 채움 / 03 기본값(DEFAULT) 열(Oracle은 LONG·ORA-00997로 별도추출 주석) / 04 FK 삭제·갱신규칙(Oracle ON UPDATE 미지원=NO ACTION 고정)
- 신규 doc 6종(no 10~15): 인덱스·제약조건·뷰·프로시저/함수/트리거·시퀀스·권한롤. 5방언. 권한롤은 실계정 익명화·결과 저장금지 경고
- UI: 사이드바 사용여부 체크박스(localStorage `deliv_use`), 하향식 그룹 소제목(준비·구조/표준화/연계/추가), 번호순 고정(체크→상단정렬 제거)
- 자체감사(P1~P7): 체크박스 `<button>`→`<div>`(무효 HTML 해소), 11 제약 Oracle 제약별 1행(CC조인 제거·fanout 해소), 10 PG INCLUDE 컬럼 제외, 14 MSSQL CAST 제거
- 외부 문서 참고 흔적 제거: `No/번호`→`순번`, '양식 열 순서'→'권장 열 구성', formCols 필수(`*`) 표시 전부 삭제. 열 내용은 일반적이라 유지

**2026-07-14**
- 임시/백업 테이블 제외 필터 신규 (02 테이블정의서 + 03 컬럼정의서 + 03 DATA_DEFAULT 병합쿼리 동시 주입 — 한쪽만 걸면 건수 불일치라 전역 1개로 강제)
- 헤더 체크박스(기본 OFF) + 세부 토글: ①`_BAK`(끝/중간) ②날짜 스냅샷(`_숫자6+` 또는 `_연도4`) ③TEST 포함(기본 꺼짐 — LATEST 오탐 위험) ④개인 접두사 입력칸(TLYH_,YDI_ 등 쉼표구분). localStorage `deliv_excl`
- "제외 목록 확인쿼리 복사" 버튼 — 걸리는 테이블을 역집합으로 조회해 오탐 검증용. 필터 켜면 쿼리 상단에 경고 주석 자동 삽입
- 방언별 구현: Tibero=REGEXP_LIKE(`{6,}`·`(19|20)` 교대 동작 확인됨 2026-07-14, 단 괄호 불일치 시 JDBC-11042) / MySQL=REGEXP / PG=`~` / **SQL Server·Sybase=정규식 미지원 → LIKE `[0-9]` 문자클래스 근사(경고 주석 자동)**. `_` 리터럴은 방언별 이스케이프(Tibero `\` / MySQL·PG `!` / MSSQL·ASE `[_]`)
- 구현: 쿼리 문자열에 `__EXCL(컬럼)__`(블록)/`__EXCLI(컬럼)__`(인라인) 토큰 → buildQuery서 치환, OFF면 흔적 없이 제거. node 문법·5방언 출력 시뮬레이션 검증 완료
- 배경: 디돌 테이블정의서 작업 중 개발자들이 백업본(`_BAK`·`_20240102_3`·이니셜 접두사)을 운영 스키마에 방치한 것 발견 — 산출물에서 걸러야 했음. 원본 공식문서는 `db_docs/` 폴더 참고

**원칙**: 스키마명·기관명은 자리표시자/입력칸. 실계정·실데이터 금지(OneDrive 동기화). 파일명 버전업 금지

---

## 산출물_sql.html의 토대 — 원본 양식 열 구성 (2026-07-14 기록)

출처: 발주처가 내려준 양식 파일 사진 13장 (`Downloads\복지부_요청_산출물\20260710_15*.jpg`).
사진에 있는 건 **03·04·05·06·07·08·09 7종**뿐 — 01(DB정의서)·02(테이블정의서)는 사진 없음(html의 formCols는 다른 근거로 작성된 것).

`(*)` = 양식 원본에 붙어 있던 **필수** 표시. html에는 외부 문서 참고 흔적 제거하며 `(*)`·`No.`를 뺐으므로(2026-07-11 이력 참고), 원본 대조가 필요할 때 아래를 볼 것.
필수 표시가 있다고 모든 행에 값이 반드시 차야 하는지는 미확인 — 발주처 확인 필요.

### 03 컬럼정의서
`DB명 | 테이블명 | 컬럼명(영문) | 컬럼명(한글) | 연관엔터티명 | 연관속성명 | Not Null | 데이터타입 | 데이터길이 | PK정보 | 제약조건 | 담당부서명 | 담당자명 | 최초등록일 | 최종수정일 | 최종수정자 | 변경구분`
- 양식 예시값: `IROSDB` / `OPENDATA_TB` / `NAME_COM` / `기관명` / `VARCHAR2` / `100` / `Y`, 일자 `2015-01-01`·`2018-04-26`, 변경구분 `신규/변경/삭제`
- 헤더행에 셀별 `M`(필수)/`O`(선택) 표기가 따로 달려 있음

### 04 테이블관계정의서
`순번 | 부모(상위) 테이블: 영문DB명 · 테이블소유자 · 한글테이블명 · 영문테이블명 · 한글컬럼명 · 영문컬럼명 | 자식(하위) 테이블: (동일 6열)`
- 양식 상단 안내문: 데이터 모델링 도구 있으면 **ERD 캡처 이미지 제출**로 갈음, 없으면 이 관계정의서 작성. 관계가 아예 없으면 사유 명시한 공식 증빙 제출
- 수준평가 확인 필수항목 = 부모/자식 각각의 한글명·영문명·한글컬럼명·영문컬럼명
- html formCols엔 `삭제규칙 | 갱신규칙`이 더 있는데 **원본 양식엔 없음** (내가 추가한 열)

### 05 DB표준용어
`No. | 선택 | 기관명(*) | DB명(*) | 표준용어명(*) | 영문명 | 영문약어명(*) | 용어설명 | 표준도메인명(*) | 허용값 | 관리부서명 | 표준코드명 | 업무분야 | 행정표준코드명 | 제정일자(*) | 특이사항`
- 2행에 `기관명 = 보건복지부`, `선택 = 0` 프리필

### 06 DB표준단어
`No. | 선택 | 기관명(*) | DB명(*) | 표준단어명(*) | 단어영문명 | 단어영문약어명(*) | 단어설명(*) | 형식단어여부(*) | 도메인분류명 | 이음동의어목록 | 금칙어목록 | 관리부서명 | 제정일자(*) | 특이사항`
- 프리필: 기관명 `보건복지부`, 형식단어여부 `N`

### 07 DB표준도메인
`No. | 선택 | 기관명(*) | DB명(*) | 표준도메인그룹명 | 도메인분류명 | 도메인명(*) | 도메인설명 | 데이터타입(*) | 데이터길이(*) | 소수점길이 | 저장형식 | 표현형식 | 단위 | 허용값 | 관리부서명 | 제정일자(*) | 특이사항`

### 08 DB표준코드
`No. | 선택 | 기관명(*) | DB명(*) | 관리부서명 | 코드명(한)(*) | 코드명(영문) | 코드설명 | 데이터타입명 | 데이터길이 | 코드값(*) | 코드값의미(*) | 코드값설명 | 제정일자(*) | 특이사항 | 사용여부`
- html formCols엔 `사용여부`가 제정일자 뒤에 있는데 원본은 맨 끝 — 순서만 다름, 내용은 동일

### 09 연계데이터 목록 정의서
2단 헤더(그룹행 + 항목행) 구조:
- **연계**: 번호 | 업무구분 | 연계정보명 | 주기 | 방식
- **연계 항목**: 컬럼명 | DB명 | 테이블명 | 컬럼명 | 데이터타입 | 데이터길이
- **제공기관(송신)**: 기관명(부처) | 시스템명
- **활용기관(수신)**: 기관명(부처) | 시스템명
- **연계기간**: 연계시작 | 연계종료
- **비고**
- 양식 예시값: `IF-MOUT-002/003/004`, 업무구분 `송신`/`수신`, 주기 `실시간`, 방식 `View Table`·`실시간`, 연계정보명 `장비등록대상`·`장비등록장(수집)`, 기관명 `보건복지부(정보통신담당관)`, 시스템명 `사회보장정보원(운영/인프라데이터 관리과)`, 연계시작 `2013-11-28`·`2014-09-02`
- 헤더행에 `필수`/`선택` 표기 별도 존재

**주의**: 위는 양식 열 구성일 뿐. 폐쇄망 DB에서 실제로 뽑을 수 있는 값과 수기로 채워야 하는 값의 구분은 `산출물_sql.html` 각 문서의 "수기 잔여 규칙" 참고.

---

## dev_tools.html 변경이력 (파일 주석에서 이관, 2026-07-11)

**2026-07-10**
- INSERT 생성기 DATE 더미값 시분초 포함(TO_DATE HH24:MI:SS 통일). 정규식 프리셋 추가(사업자번호 하이픈무관·법인번호·전화·주민번호). Base64 "파일→data URI"(드래그&드롭). JSON 16자리↑ 숫자 정밀도 경고
- data URI: 이미지 미리보기 + `<img>` 복사 버튼(이미지일 때만)
- 버튼 상단 통일. 정규식 프리셋 결과영역 위로. 정규식·카멜 ▷실행 버튼 제거(자동실행)
- SQL 페이징 DB 5종 확장(+SQL Server OFFSET FETCH·ORDER BY 누락경고, +Sybase IDENTITY 임시테이블). 옵션줄 통합. COUNT 쿼리 체크(기본 켜짐). Oracle OFFSET FETCH 옵션(12c+/Tibero6+, 기본 ROWNUM 11g호환)
- SQL 페이징 DB선택 localStorage. 세미콜론 자동 정리
- 파일명 `dev_tools_v3`→`dev_tools`. localStorage 접두사 `devt3_`→`devt_`
- AI 교차검증 [1]~[8]: parseCreate 주석제거+괄호깊이 콤마분할(NUMBER(10,2) 콤마·주석에 컬럼 유실되던 것), URL searchParams decodeURIComponent 중복제거, XSS esc() 전역화(URL·JWT), 날짜 fmtEpoch 통일+KST 라벨, Unix 자릿수 ms 자동판정, 정규식 프리셋 교정, URL-safe Base64 디코딩, JWT 파트오류 표시
- INSERT 더미값 품질(교차검증 7): parseCreate 제약수집(PK/UNIQUE/FK/NOT NULL/DEFAULT/CHECK IN), CHECK IN 목록 값만 생성, PK/UNIQUE 숫자 순번, NUMBER(p,s) 반영, 날짜 상대생성, UPSERT PK 탐색+경고, FK·복합CHECK 경고주석. DB 5종 분기(날짜리터럴/UPSERT/트랜잭션종료문)

## sql_snippets.html 변경이력 (파일 주석에서 이관, 2026-07-11)

**2026-07-10**
- Oracle 컬럼목록 코멘트 조인, 최근변경객체 LAST_DDL_TIME, 그룹핑 카운트 템플릿(Oracle/MySQL/PG), 바인드변수(`#{param}`·`:param`) 하이라이트
- DB 탭 5종 확장(산출물_sql과 분류 통일, 내부키 oracle/mysql/pg 유지). MySQL 락조회 5.7/8.0 병기. 파일명 `sql_snippets_v2`→`sql_snippets`
- '환경/시간' 카테고리 추가(5탭 전부) — DB 시간·타임존 확인

**2026-07-11**
- 커스텀 스크롤바 추가(산출물_sql과 통일)

## 특수문자_모음.html 변경이력 (파일 주석에서 이관, 2026-07-11)

**2026-07-09** 최초 작성
**2026-07-10**
- 즐겨찾기(우클릭 토글, localStorage, 상단 고정), 조합모드(클릭 누적→한번 복사), 선문자 카테고리
- 항목 기호 체계 카드(공문서 8단계 / 조문형식 조·항·호·목 토글), 연속복사, 접기/펼치기. 출처 `project_doc_format_rules.md §1`
- AI 교차검증 [1]~[5]: 하단바 조합문자 코드포인트 전부 나열, 조합↔연속복사 상호배타, 검색별칭(1→①⑴Ⅰ, 가→㉮), 즐겨찾기 배열전달(공백항목 파손방지), localStorage 접두사 `spc_` 통일

**2026-07-11**
- 라이트 우선 → 다크 우선 전환(팔레트·폰트·accent를 index/다른 도구와 통일), 커스텀 스크롤바(다크/라이트 대응). 카드그리드 구조는 유지

## jsp_formatter.html 변경이력 (파일 주석에서 이관, 2026-07-11)

**2026-07-09** 최초 작성
**2026-07-10** 알려진 한계 문서화(파일 주석 + 화면 하단 안내)

**알려진 한계**
1. 인라인 요소(`<span>`,`<a>`) 사이 공백 재배치가 렌더 간격에 영향 가능 → 정리 후 육안 확인, "짧은 태그 한 줄 유지" 옵션 조정
2. JSTL은 정상 들여쓰기. 단 여닫음 비대칭 레거시(테이블 행을 스크립틀릿으로 반쪽만) 구간은 깊이 틀어질 수 있음 → 수동 정리
3. `<script>` 내부 JS는 원형 유지만(들여쓰기 이동만, JS 포매팅 없음)

개선 후보(미구현): 정리 전/후 diff 뷰(공백 외 변경 0 검증), 파일 드래그&드롭, 일괄 처리

## index.html 변경이력 (파일 주석에서 이관, 2026-07-11)

**2026-07-10** 최초 작성 — 도구 카드 런처, 새 탭 열기. 폴더째 복사 후 이 파일만 즐겨찾기. 새 도구는 `TOOLS` 배열에 한 줄 추가. 외부 CDN 금지
