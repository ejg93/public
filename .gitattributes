# 텍스트 파일 줄바꿈은 저장소에 LF 로 저장한다
* text=auto

# 셸 스크립트는 작업 트리에서도 LF 를 유지해야 한다.
# CRLF 가 섞이면 Git Bash 가 shebang 을 못 읽고 bad interpreter 로 죽는다.
mvnw text eol=lf
*.sh text eol=lf

# 윈도우 배치 파일은 CRLF
*.cmd text eol=crlf
*.bat text eol=crlf
